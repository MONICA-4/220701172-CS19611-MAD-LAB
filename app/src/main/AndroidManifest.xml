<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Eventbookingapp"
        tools:targetApi="31">

        <!-- MainActivity Declaration -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Eventbookingapp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- BookingActivity Declaration -->
        <activity
            android:name=".BookingActivity"
            android:exported="true"
            android:label="Booking Event"
            android:theme="@style/Theme.Eventbookingapp"/>

        <!-- InvoiceActivity Declaration -->
        <activity
            android:name=".InvoiceActivity"
            android:exported="true"
            android:label="Invoice"
            android:theme="@style/Theme.Eventbookingapp"/>

        <!-- ViewBookingsActivity Declaration -->
        <activity
            android:name=".ViewBookingsActivity"
            android:exported="true"
            android:label="Your Bookings"
            android:theme="@style/Theme.Eventbookingapp" />


    </application>

</manifest>
